{
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "playwright-bdd": "^8.4.0"
  },
  "scripts": {
    "cadastro": "npx bddgen && npx playwright test --grep @cadastro"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  }
}
